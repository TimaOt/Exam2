#include <iostream>
#include <time.h>
#include "Header.h"	
using namespace std;

void main()
{
	setlocale(0, "");
	srand(time(0));
	int kol = 10;
	while(true)
	{
		int x;
		cout << "Выберите действие:" << endl;
		cout << "1:Показать все квартиры:" << endl;
		cin >> x;
		switch (x)
		{
		case 1:
			for (int i = 0;i < 10;i++)
			{
				cout<< 
			}
			break;
		}

	}
	system("pasuse");
}